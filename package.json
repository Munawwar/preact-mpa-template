{
  "name": "preact-mpa-template",
  "version": "0.0.1",
  "description": "Preact multi-page app/website starter template (SSR) - with Fastify and rsbuild",
  "type": "module",
  "module": "server/server.js",
  "scripts": {
    "dev": "NODE_ENV=development mkdirp dist && npm run build:ssr && npm run watch:server",
    "watch:server": "NODE_ENV=development nodemon --enable-source-maps --import dynohot --watch server/routes/routes.js --watch server/paths.js --watch server/server.js --watch rsbuild.config.js server/server.js",
    "build:ssr": "node build.js --ssr-only",
    "build": "node build.js",
    "build:client": "node build.js",
    "build:watch": "node build.js --watch",
    "build:dev": "node build.js --dev --watch",
    "prod": "NODE_ENV=production node --enable-source-maps server/server.js",
    "lint": "eslint --ext js,jsx build.js server/ client/ rsbuild*.config.js",
    "lint:fix": "eslint --fix --ext js,jsx build.js server/ client/ rsbuild*.config.js"
  },
  "keywords": [
    "preact",
    "MPA",
    "SSR",
    "rsbuild"
  ],
  "author": "Munawwar",
  "license": "MIT",
  "dependencies": {
    "@fastify/compress": "^8.0.1",
    "@fastify/middie": "^9.0.2",
    "@fastify/static": "^8.0.3",
    "fastify": "^5.2.0",
    "html-safe-json": "^2.0.3",
    "preact": "^10.12.1",
    "preact-render-to-string": "^5.2.6"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.1.8",
    "@rsbuild/plugin-preact": "^1.1.8",
    "concurrently": "^7.6.0",
    "dynohot": "^1.2.1",
    "eslint": "^8.32.0",
    "eslint-config-preact": "^1.3.0",
    "eslint-config-semistandard": "^17.0.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-jsx-a11y": "^6.7.1",
    "eslint-plugin-n": "^15.6.1",
    "eslint-plugin-promise": "^6.1.1",
    "mkdirp": "^3.0.1",
    "nodemon": "^3.1.9",
    "tiny-glob": "^0.2.9"
  },
  "eslintConfig": {
    "ignorePatterns": "dist/",
    "env": {
      "browser": true,
      "node": true,
      "es2021": true
    },
    "extends": [
      "semistandard",
      "preact",
      "plugin:jsx-a11y/recommended"
    ],
    "overrides": [],
    "parserOptions": {
      "ecmaVersion": 2021,
      "sourceType": "module",
      "ecmaFeatures": {
        "jsx": true
      }
    },
    "rules": {
      "semi": "error"
    },
    "settings": {
      "jest": {
        "// jest is dummy config only needed for preact eslint plugin": true,
        "version": 27
      }
    }
  }
}
